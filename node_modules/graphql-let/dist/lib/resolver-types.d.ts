import { PartialCodegenOpts } from './create-codegen-opts';
import { CommandOpts, ConfigTypes } from './types';
export declare function shouldGenResolverTypes(commandOpts: CommandOpts, config: ConfigTypes): boolean | undefined;
export declare function processGenerateResolverTypes(cwd: string, config: ConfigTypes, codegenOpts: PartialCodegenOpts): Promise<{
    tsxFullPath: string;
    dtsFullPath: string;
    gqlRelPath: string;
}>;
